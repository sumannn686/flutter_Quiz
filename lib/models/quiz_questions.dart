//this is the class --blueprint
// this is called models for qustions and answer
class QuizQuestion {
  const QuizQuestion(this.text, this.answer);

  final String text;
  final List<String> answer;
}
